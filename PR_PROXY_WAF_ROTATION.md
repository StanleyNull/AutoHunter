## 为什么要加

挖漏洞易会触发 WAF，后面的请求全部被拦截。现在可以在项目里配置多个代理，遇到这种情况自动换一个出口继续测试。

## 做了什么

- 在设置页增加代理池配置，可以添加、编辑、删除、导入和测试代理。
- Worker 第一次发请求时，会从代理池分配一个可用代理。
- 如果代理本身连接失败，会自动换下一个代理重试。
- 如果响应明确显示是 WAF 拦截，系统会把当前代理暂时记为已用，随机换一个可用代理，并自动重试原请求一次。
- 换代理时会保留当前 Cookie 和登录状态，不需要重新登录。
- 没有可用代理时继续使用直连，不会因为代理池耗尽导致任务直接中断。
- 普通的权限不足、路径不存在或普通 `403 Forbidden` 不会被误认为 WAF，也不会触发换代理。

## 使用方式

1. 打开设置页的「代理」标签。
2. 添加代理，或按支持的格式批量导入。
3. 打开代理池总开关，并先点击测试确认代理可用。
4. 启动挖掘任务。遇到 WAF 封禁时，系统会自动尝试切换出口。


## 验证

```bash
python -m unittest tests.test_proxy_executor tests.test_proxy_service
python -m unittest discover -s tests -p 'test_proxy*.py'
```

新增测试覆盖：WAF 响应触发随机代理并重试、普通 Forbidden 不误触发、随机候选选择。

## 风险与兼容性

- 每个请求最多因 WAF 自动重试一次，避免代理池耗尽或请求风暴。
- 只有识别到明确拦截证据才自动切换，业务权限错误不会被隐藏。
- 代理池关闭或为空时维持直连行为。