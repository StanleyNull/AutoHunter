# AutoHunter 挖洞能力提升分析

> 重新界定：用户要的不是"统计一个成功率数字"，而是**如何让平台实际挖到更多、更深的洞**——即挖洞能力（命中率/质量）的上限在哪里、当前被什么卡住、怎么提。  
> 分析版本：仓库根 `app/`（较新工作副本）

---

## 0. 先说结论

AutoHunter 的挖洞能力 = **方法论 prompt × 工具集 × 情报复利 × 放大机制 × 模型质量**，再被**收敛护栏**削掉一部分深度。它已经比"裸 LLM + 几个工具"强很多：有一套从生产驳回库沉淀出的"出洞铁律"，有 Cookie 自动吸收/SSO 链路、JS 审计、WAF 建议、资产测绘、查重、情报沉淀等工具，还有打法路由、单站多路线协同、通杀扩散。

**真正卡住能力的不是"不会挖"，而是三件事：**

1. 方法论全压在 prompt 文本里，靠 LLM 自觉遵守 → 弱模型直接塌；
2. 收敛/轮次/回炉上限是**硬上限**，复杂利用链（指纹→登录→IDOR→读数据）常在成型前被截断；
3. 反馈闭环太浅——人工采纳/打回的结论没有回流成系统性的能力提升（情报库只沉淀凭证/端点，不沉淀"打法"）。

---

## 1. 项目已经具备的"挖洞能力底座"（值得肯定）

### 1.1 经验型方法论 prompt（最强资产）

`app/agents/prompts.py:178-208` 的 Worker 系统提示写得很实战：

- **出洞铁律**：逻辑洞优先（弱口令/已知 CVE 最低价值，只试一次不中即弃），优先认证绕过/参数覆盖/SSO bypass/未授权/IDOR/接管/注入/上传；
- **打穿门限**：发现攻击面绝不停在半成品，必须追问"然后呢？能打穿吗"，升级到"够格的东西"；
- **禁止扫描器心智**：不要把目标交给 nuclei/sqlmap/nmap 等结果，这不是挖洞。
- **生产驳回反例同步**：`prompts.py:151-164` 直接内置"2026-06 同步生产驳回库"的真实反例（Druid 只泄露 JDBC 不算、RSA 私钥只解密出 401 不算、CAS 登录进个人中心不算……）。这是用真金白银的驳回经验把 Reviewer 调准。

### 1.2 工具集（能力底座扎实）

`app/tools/schemas.py` 暴露给 Worker 的工具远不止发包：

- `http_request`：自动吸收 Cookie（含整条 302 重定向链每一跳的 Set-Cookie），CAS/SSO 一次 POST 走完 `lt→CASTGC→ST→JSESSIONID`——**把最难的登录链做成了一行参数**；
- `analyze_javascript`：审计前端 JS/接口/硬编码密钥/路由，给线索地图；
- `suggest_waf_bypass`：被 403/406/429 时本地识别 WAF 指纹并给候选变形（再实证）；
- `fofa_lookup`：只读测绘，找同 IP/同域隐藏攻击面 + 确认归属；
- `decode_transform`：JWT/哈希/base64 本地解析，零副作用；
- `check_duplicate_finding` / `submit_finding` / `report_intel`：查重、带 kill_chain+self_check 出洞、情报沉淀。

### 1.3 打法路由（让"进去后先打哪条路"有依据）

`app/agents/playbook_router.py`（511 行）：把资产信号（Swagger/Actuator/Nacos/GraphQL/DevOps/对象存储…）映射到更窄的 worker 挖掘路线，基于 OWASP API Top10 / WSTG 语义，输出"focus / avoid / finish_hint"注入 Worker prompt，不替代 scorer，但决定进去先打哪。

### 1.4 单站多路线协同（深目标的杀手锏）

`app/agents/site_collab.py`：单站任务把同一真实主机分给多条 focused worker——入口/API 盘点、前端 JS/密钥、认证绕过、文件上传、注入、逻辑——各自 coverage 反馈给团队。**这是批量 FOFA 任务没有的深度**。

### 1.5 情报复利（越挖越值钱）

`app/agents/intel_curator.py` + `report_intel`：验证过的凭证(cred)/有效端点(endpoint)/技术栈画像(profile)入全局库，后续同类系统 worker 直接复用。`intel_curator` 用确定性规则拦垃圾（未验证凭证/公开静态浅路径/占位画像/失败结论），保守不误删。

### 1.6 放大机制（单洞→一批）

- `deepen` 回炉（`deepen.py`）：AI 打回 / 人工"继续深挖"把原 finding 标 superseded，目标带定向指令拉回队首；
- `killsweep` 通杀（`killsweep.py`）：已采纳洞认指纹→FOFA 圈同款→实打 2~4 个验证→判定能否一打一片；
- `escalate` 扩大危害。

### 1.7 长程记忆

`history.py` 的 compact + `resume_context`：多轮上下文压缩与跨轮会话态保持，让长利用链不断。

---

## 2. 真正限制挖洞能力上限的短板（Deficiencies）

| # | 短板                   | 证据位置                                                           | 对"挖洞能力"的影响                                    |
| - | -------------------- | -------------------------------------------------------------- | --------------------------------------------- |
| A | **方法论靠 LLM 自觉遵守**    | `prompts.py:178-208` 全是文本指令；`worker.py:397-422` 哑模型零工具即收尾      | 弱模型不 follow 铁律 → 能力塌陷；同一套 prompt 在不同模型上能力方差极大 |
| B | **收敛/轮次是硬上限**        | `worker.py:269` edu 90/45、企业 110/60 轮；`worker.py:481-504` 自动收尾 | 复杂多步链（指纹→登录→IDOR→读数据）常在成型前被截断，深度被锁死           |
| C | **回炉深挖上限仅 2 次**      | `deepen.py:13` `DEEPEN_CAP_DEFAULT=2`                          | 多步链 2 轮打不穿即归档，无法接力把入口补成实锤                     |
| D | **Reviewer 过杀中低危**   | `reviewer.py:48-54,195-234` 一整套 ignored 规则                     | 挖到了却被 kill，等效降低"有效能力"；弱但真实的洞大量被丢              |
| E | **反馈闭环浅（不沉淀打法）**     | 情报库只存 cred/endpoint/profile（`intel_curator.py`）；铁律靠人工同步生产驳回库   | 成功经验没有抽象成"攻击链模板"跨目标复用；系统不会"越挖越聪明"             |
| F | **无难度分级模型路由**        | `worker.py` 全程单模型；仅 `no_tool_rounds` 自愈换端点                     | 简单目标用强模型浪费、难目标用弱模型挖不动；无"信号强就升级模型再挖一轮"         |
| G | **批量任务无单站级深度**       | site_collab 是独立 source；FOFA 批量每目标仍单 worker 单 pass              | 高价值指纹目标没自动升级成多路线深挖，潜力浪费                       |
| H | **工具广度被刻意收窄**        | `worker.py:1088-1131` 禁泛扫；`schemas.py:33` run_shell 禁空转        | 安全/成本好，但 discovery 全靠 LLM 临场，漏扫风险高            |
| I | **打法路由是"建议"非"强制流程"** | `playbook_router.py` 输出注入 prompt，不强制步骤                         | 弱模型可能忽略路线，关键侦察步骤漏做                            |
| J | **对抗 WAF/风控仅文字建议**   | `suggest_waf_bypass` 只给候选变形，不自动降速/换路                           | 真实目标有 WAF/限频/IP 封锁时易被拦，无自动换出口                 |



---

## 3. 提高挖洞能力的具体改造（对应上表）

**C1（治 A/I，收益最大、最优先）把"必做侦察"从 prompt 建议升级为确定性前置剧本**

- 不靠 LLM 自觉：所有目标先确定性跑一套"侦察基线"——robots/sitemap/JS 提取/常见后台/API 文档/指纹识别/敏感端口探测，结果作为结构化上下文喂给 Worker。
- 这样无论模型强弱，关键入口都不会漏。playbook_router 的路线也从"建议"变成"带 checkpoint 的流程"。

**C2（治 B/C，释放深度）让代码尊重"打穿优先于轮次纪律"**

- prompt 已写"已拿到据点时打穿优先于轮次纪律"（`prompts.py:183`），但 `worker.py` 的 rounds 是硬上限。对"已得据点"（注入点/未授权口/LFI/上传点/可控 token）的目标，自动追加 rounds 预算、放宽自动收尾；把 `deepen` 上限从 2 提到 3~4，让多步链能接力。

**C3（治 F，模型杠杆）难度分级模型路由**

- 按目标价值/已得线索强度路由模型：普通目标便宜模型，高价值/已得据点目标强模型；收敛前若信号强，自动升级模型再挖一轮（复用现有 `LLM_PROVIDER_POOL` 机制）。

**C4（治 E，复利升级）把"打法"也变成可复用资产**

- 在情报库之外加一层 **攻击链模板库**：把已验证成功的 `kill_chain` 抽象成"指纹 X → 步骤 → 实锤条件"模板，新同类目标自动套用（不只复用凭证/端点，复用"怎么打"）。
- 把人工"采纳/打回"沉淀为可查询经验库，定期回流修订"出洞铁律"和 Reviewer 规则（现在靠人工手同步，可自动化）。

**C5（治 G/D，放大深度与召回）批量任务识别高价值指纹后自动升级单站多路线**

- 对 FOFA 批量里命中高价值指纹（Druid/Nacos/Actuator/Swagger/已知产品）的目标，自动转成 site_collab 式多 worker 深挖，而不是单 pass 草草收尾。
- 同时降低 Reviewer 过杀：中危且证据完整也跑轻量复现，给"弱但真实"的洞留 `deepen` 而非 `ignored`（D4）。

**C6（治 J/H，对抗与发现）WAF 感知 + 适度结构化发现**

- `suggest_waf_bypass` 升级为自动：检测到 403/WAF 特征时自动降速、轮换 UA、切换代理出口，而非只给文字建议。
- 在"确定性侦察基线"内允许受控的指纹级探测（不泛扫，但按已知高危路径定向验证），补足纯 LLM 临场的漏扫。

---

## 4. 改造优先级（先做什么最赚）

1. **C1 确定性侦察基线 + C2 解除据点目标的轮次/回炉硬上限**——直接把"漏挖"和"半途而废"两道最大内耗干掉，几乎不依赖换模型就能提命中率。
2. **C4 攻击链模板库 + 经验回流**——让系统"越挖越聪明"，是复利型长期收益。
3. **C3 难度分级模型路由**——用最省的 token 把难目标交给强模型，性价比最高。
4. **C5 批量高价值指纹→单站多路线 + Reviewer 降过杀**——放大单洞产出。
5. **C6 WAF 感知/对抗**——解决真实目标拦不住的问题。

> 一句话：**AutoHunter 的"脑子"（方法论+工具）已经不错，瓶颈在"手"（收敛硬上限、弱模型不 follow、反馈不闭环）。优先做 C1+C2 把深度放出来，再做 C4 把经验复利起来，挖洞能力会有实质性跃升，而不只是换个更强的模型。**
